import 'package:flutter/material.dart';

class NoteModel {
  final String title, subTitle, data;
  final Color color;

  NoteModel({required this.title, required this.subTitle, required this.data, required this.color});
  
}
